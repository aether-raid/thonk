import type { CameraConfig } from "@/types/ppg";

export const CAMERA_CONFIG: CameraConfig = {
  width: 1280,
  height: 720,
};

export const PULSE_CONFIG = {
  frameProcessingQuality: 0.8,
  frameRate: 30, // Approximate frame rate for processing
};
